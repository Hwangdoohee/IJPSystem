<Window x:Class="IJPSystem.Platform.HMI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="System Login" Height="250" Width="380" 
        WindowStyle="None" ResizeMode="NoResize" 
        WindowStartupLocation="CenterOwner"
        Background="#1E293B" BorderBrush="#3B82F6" BorderThickness="2">

    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="SYSTEM LOGIN" Foreground="White" FontSize="20" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,25"/>

            <TextBlock Text="PASSWORD" Foreground="#94A3B8" FontSize="11" FontWeight="Bold" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordInput" Height="40" FontSize="18" VerticalContentAlignment="Center" 
                         Background="#0F172A" Foreground="White" BorderBrush="#334155" Padding="10,0"/>

            <Grid Margin="0,25,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="CANCEL" Click="Cancel_Click" IsCancel="True"
                        Height="40" Margin="0,0,5,0" Background="#475569" Foreground="White" 
                        FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Grid.Column="1" Content="LOGIN" Click="Login_Click" IsDefault="True"
                        Height="40" Margin="5,0,0,0" Background="#3B82F6" Foreground="White" 
                        FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </StackPanel>
    </Grid>
</Window>